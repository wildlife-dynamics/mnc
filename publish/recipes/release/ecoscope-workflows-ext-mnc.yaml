context:
  name: ecoscope-workflows-ext-mnc
  version: ${{ env.get("HATCH_VCS_VERSION") }}


package:
  name: ${{ name }}
  version: ${{ version }}


source:
  path: ../../..  # get back to root of repo

build:
  noarch: python
  script: SETUPTOOLS_SCM_PRETEND_VERSION=${{ version }} python -m pip install src/ecoscope-workflows-ext-mnc -vv --no-deps
  number: 0

requirements:
  host:
    - python
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python
    - wt-registry >=0.2.0,<0.3.0
    - wt-task >=0.1.0,<0.2.0
    - ecoscope-platform >=2.15.0,<3.0.0
    - ecoscope
    - ecoscope-workflows-ext-custom
    - geopandas
    - shapely
    - docxtpl
    - playwright
    - numpy
    - setuptools <=81.0.0
    - docxcompose
about:
  license: BSD-3-Clause

extra:
  recipe-maintainers:
    - tevin-temu
